parameters:
  enabled: ''

steps:
  condition: succeeded()
- template: ./steps/gradle-task.yaml
  parameters:
    gradleTasks: 'pitest'
    displayName: 'Pitest analisys'
  extraOptions: '--build-cache'